//  request for user data for calculation of tax
function basicSalary(){
    let amount=parseFloat(prompt(`Please enter you basic salry: `));
    let benefits=parseFloat(prompt(`Please enter the amount of benefits you earn: `))
    return amount;
}
function payeCalculator(paymentPeriod,grossSalary,contBenefit){

}